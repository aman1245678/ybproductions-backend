## Change
- [ ] `feat` / `fix` / `test` — one behavior

## Tests
- [ ] Matching `*.test.mjs` or `*.spec.ts` is in this PR
- [ ] `npm run test:server` passes (backend-only, no Angular build)

## Notes
PRs without tests will not be merged.
